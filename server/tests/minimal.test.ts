// Minimal test file to verify Jest setup
console.log('Minimal test file is running');

test('should pass a simple test', () => {
  console.log('Running minimal test...');
  expect(1 + 1).toBe(2);
});
